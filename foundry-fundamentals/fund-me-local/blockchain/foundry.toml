
[profile.default]
src = "src"
out = "out"
libs = ["lib"]

remappings = ["@chainlink/=lib/chainlink-brownie-contracts/"]

fs_permissions = [
    { access = "read", path = "./broadcast" },
    { access = "read", path = "./reports" },
]

[rpc_endpoints]
ethereum = "${ETHEREUM_RPC_URL}"
ethereum-sepolia = "${ETHEREUM_SEPOLIA_RPC_URL}"
base-sepolia = "${BASE_SEPOLIA_RPC_URL}"
zksync-sepolia = "${ZKSYNC_SEPOLIA_RPC_URL}"
avalanche-fuji = "${AVALANCHE_FUJI_RPC_URL}"
anvil = "http://127.0.0.1:8545"
anvil-zksync = "http://0.0.0.0:8011"

[etherscan]
ethereum = { key = "${ETHERSCAN_API_KEY}" }
ethereum-sepolia = { key = "${ETHERSCAN_API_KEY}" }

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
